name: Build 32bit Linux Binary
on: [workflow_dispatch]
jobs:
  build_linux_binary:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3
      - name: Install gcc
        run: sudo apt-get install gcc gcc-multilib g++-multilib binutils -y
      - name: Cache 32bit linux cargo
        uses: actions/cache@v3
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-32bit-${{ hashFiles('**/Cargo.lock') }}
      - name: Install rust target i686-unknown-linux-gnu
        run: rustup target add i686-unknown-linux-gnu
      - name: Install cargo make
        run: if [[ ! -e ~/.cargo/bin/cargo-make ]]; then cargo install cargo-make; fi
      - name: Set permissions for script
        run: sudo chmod +x scripts/build-i686-unknown-linux-gnu.sh
      - name: Build with cargo-make
        run: cargo make -e CARGO_MAKE_RUST_TARGET_TRIPLE=i686-unknown-linux-gnu --profile=release
      - name: Upload artifact
        uses: actions/upload-artifact@v2
        with:
          name: bfc-i686-unknown-linux-gnu
          path: output/bfc-i686-unknown-linux-gnu